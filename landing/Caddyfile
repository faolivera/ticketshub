# Serve static files. localhost always works (for curl/debug on server).
# Set DOMAIN=ticketshub.shop so Caddy also serves and gets HTTPS for that hostname.
localhost {$DOMAIN:} {
	root * /srv
	encode gzip
	file_server
}
